const mystr = "This is my country";

// Count the total length of the string (including spaces)
console.log(mystr.length);

// Split the string into an array of words based on spaces
const str = mystr.split(" ");

console.log(str.length);